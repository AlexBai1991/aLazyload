var webpack = require('webpack');
var wbpConfig = require('./webpack.config.js');
// modify output.filename[no more modifing here.]
// wbpConfig.output.filename = 'test.min.js';
// modify plugins
wbpConfig.plugins =  (wbpConfig.plugins || []).concat([
  new webpack.optimize.UglifyJsPlugin({ minimize: true })
]);

module.exports = wbpConfig;